@model IEnumerable<AMST4.Carousel.MVC.Models.Product>

@{
    ViewData["Title"] = "Product List by Size";
}

<h1>Lista de Produtos por Tamanho</h1>

<form method="get" asp-action="ProductListByCategory">
    <div class="form-group">
        <label for="sizeId">Tamanho</label>
        <select id="sizeId" name="sizeId" class="form-control" asp-items="ViewBag.SizeList">
            <option value="">Selecione um Tamanho</option>
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Filtrar</button>
</form>

<hr />

@if (Model.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Descrição</th>
                <th>Imagem</th>
                <th>Ativo</th>
                <th>Categoria</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var product in Model)
            {
                <tr>
                    <td>@product.Name</td>
                    <td>@product.Description</td>
                    <td>
                        <img src="~/@product.ImageUrl" alt="Imagem do Produto" class="img-fluid" style="max-width: 100px;" />
                    </td>
                    <td>@product.IsActive</td>
                    <td>@product.Category.Name</td>
                    <td>@product.Category.Description</td>

                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Nenhum produto encontrado para o Tamanho Selecionado selecionada.</p>
}
